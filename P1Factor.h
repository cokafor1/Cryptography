#pragma once
#include <iostream>
#include <cstring>
#define ll long long

using std::cout;
using std::cin;

ll f(ll a, ll b, ll c);
ll gcd(ll a, ll b); //gcd declaration
ll factor(ll n, ll k);
